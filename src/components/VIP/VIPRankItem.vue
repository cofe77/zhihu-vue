<template>
  <div
    class="vip-rank-item"
    :style="{ backgroundImage:`url(${src})` }"
    @click="handleVIPRankClick"
  >
    <div class="vip-mark">
      VIP
    </div>}
    <div class="vip-footer">
      <slot />
    </div>
  </div>
</template>

<script lang="ts" setup>
import { toRefs } from 'vue'
interface VIPRankItemType {
  src: string,
}
const props = defineProps<VIPRankItemType>()
const { src } = toRefs(props)

const handleVIPRankClick = () => {
}
</script>

<style lang="less">
.vip-rank-item{
  width: inherit;
  height: inherit;
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  cursor: pointer;
  border: 3px solid #fff;
  .vip-mark{
    width: 35px;
    height: 20px;
    background: linear-gradient(98deg,#f8e2c4,#f3bb6c);
    position: absolute;
    top: 0;
    left: 0;
    border-radius: 0 0 10px 0;
    text-align: center;
    color: #754e19;
    font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
  }
  .vip-footer{
    position: absolute;
    bottom: 0;
    right: 0;
    background-color: hsla(0,0%,100%,.8);
    border-radius: 10px 0 0 0;
    padding: 0 3px;
    font-size: 12px;
  }
}
</style>
